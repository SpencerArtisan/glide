resources.createImageSprite("hello-world.png")
     .setPosition(180, 180)
     .setColour(Red)

resources.createImageSprite("hello-world.png")
     .setPosition(400, 420)
     .setColour(Green)

resources.createImageSprite("hello-world.png")
     .setPosition(620, 180)
     .setColour(Blue)

screen.update()
