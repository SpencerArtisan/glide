# Cameras allow you to zoom in and out...
camera.zoom = 2

# ...or to shift the screen
camera.x = camera.x + 10
camera.y = camera.y + 4

# ...even rotate the whole screen
camera.angle = camera.angle + 5

# When moving the camera, you may want some sprites to NOT move.
myStationarySprite.setLayer(Overlay)
