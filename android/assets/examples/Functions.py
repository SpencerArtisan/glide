def controlCar():
    # This function will contain lots of code about controlling the car

while screen.update():
    controlCar()

# Functions can also take in values
def moveShip(amount):
    ship.x = ship.x + amount

moveShip(100)
