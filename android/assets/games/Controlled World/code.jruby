#############################################
#         Welcome to Planet Burpl!          #
#                                           #
# Use KEYBOARD to use keys to control stuff #
#############################################

world = $blurp.createImageSprite "world.png"
while true do
    world.x(200) if ($keyboard.isKeyPressed(Key::Left))
    world.x(500) if ($keyboard.isKeyPressed($keys.Right))
    $blurp.blurpify
end
